
> **2026-08-28 三轮升级**：融入三份新资料全量知识（已精读蒸馏）——
> ①《第一讲 赛题识别与模型选型》→ **`21_赛题识别八类任务判据.md`**（破题/选型第一裁决：40分钟流程、八类判据+必报量、四组易混、外推10%判据、优化规模分档、线性化三招、整卷统筹两接口、选题概率账）；
> ②《数学建模流程化解题思路》→ **`22_题型流程化解题手册.md`**（七类题型完整作业流程：预处理顺序/模型选择条件表/必做检验/不推荐清单）；
> ③《国赛论文模版（最强版）》批注 → 已并入 `10`（摘要四步流程、死亡图禁区、图表公式数量基准、灵敏度三档、优缺点比、参考文献4~10条+AI版本号）、`07` §3（假设六类与排序）与 `13b`（C题趋势、选题概率账）；2026-08-29 正文全量复核，数量与内容标准另入 `25` §0B（全卷一张 C050 式总流程图）。
> 阶段①②③④⑤ 的执行指令已同步接线（stage1~4、07、09、10、05、13b）。

> 设计目标：**以后做建模/写国赛论文，照这个流程跑，负担最轻**。
> 配套资产在同目录：`01~06`。先读完本文件，再按计划走。

---

## 一、总体流水线（6 阶段 + 1 个特殊模式）

```
[准备期] 我建好作战包 + 你丢材料(题目/原论文/数据集)
   ↓
① 破题与契约      写「研究契约」+「Claims-Evidence 矩阵」锁范围
   ↓
② 建模            按题型选模型 + 写「实验/求解计划」（核心问设 bake-off 选拔赛节 + 逻辑合同）
   ↓
③ 代码与求解      跑数据 + 7 类自检 + 逻辑体检闸 + 数字进 all_results.json
   ↓
④ 写论文          按国赛样式 + 7 条去AI铁律 + 图题≤15字禁公式
   ↓
⑤ 自动评审循环    我当审稿人挑刺（+32号评分视角 + 逻辑对抗复核）→ 迭代改稿直到通过
   ↓
⑥ 交付            md → build_docx.py（套 17 规范填国赛 Word 原模板）→ .docx → validate_docx_format.py 程序化校验 → 导出 PDF（详见 14_编译与提交说明.md；cumcmthesis LaTeX 仅作备选）
   ↓
[改进论文模式] 输入原优秀论文 → 找不足 → 改出含「修改总概况」的改进稿
```

1. **先契约后动手**（避免跑偏、上下文污染）
2. **数字可追溯**（一切来自 `all_results.json`，不手编）
3. **让另一个模型审上一个模型的产出**（自动评审循环）

---

## 二、每个阶段"我做什么 / 你做什么"

| 阶段 | 我（主 agent）做 | 你（队长）做 | 产出 |
|---|---|---|---|
| 准备期 | 建作战包、配样式、写评审清单 | 把「题目 / 原优秀论文 / 数据集」发我 | 材料齐 |
| ① 契约 | 读题、写研究契约、列 Claims-Evidence | 确认/纠偏范围 | `研究契约.md` |
| ② 建模 | 用 math-modeling-solver 决策矩阵推模型、写求解计划（**按 `07_技能_comp-modeling.md`**） | 选模型方向 | `求解计划.md` |
| ③ 求解 | 写 Python 跑数据、做七类自检、存 JSON（**按 `09_技能_comp-code.md`**） | 提供/核对数据 | `all_results.json` + 图 |
| ④ 写稿 | 按 `competition_zh.json` 样式写、守铁律、表格取自 JSON（**按 `10_技能_comp-paper.md`**） | 审内容准确性 | `论文.md` |
| ⑤ 评审 | 当审稿人按 `05_自动评审清单` + **`08_技能_auto-review.md`** 挑刺、改稿迭代 | 看评审意见、定夺 | 定稿 + `frozen_numbers.json`（`scripts/freeze_numbers.py freeze` 数字冻结） |
| ⑥ 交付 | **先 `freeze_numbers.py check` 零漂移对账（漂移即停：PROGRESS.md 记原因→重跑受影响章节评审→重新 freeze）**，再用 `build_docx.py` 把 `论文.md` 转套用 `17` 规范的 .docx、跑 `validate_docx_format.py` 程序化核验（fail==0 才过）、脱敏+命名+导出 PDF（**按 `14_编译与提交说明.md`**）；cumcmthesis LaTeX 仅作备选 | 交稿 | 论文.docx / 论文.pdf |
| 改进模式 | 见 `06_改进论文流程.md` | 给原论文+题+数据 | 改进论文 |

---

**阶段 ↔ 已装数学建模 skill 调用映射**（真 skill 已装在 ZCode `~/.zcode/skills/` 即本作战包同级目录，是方法主力；作战包 07~10 是国赛专项约束层，二者互补不冲突）：

| 阶段 | 主力方法（已装 skill） | 国赛专项约束（作战包文件） | 配合方式 |
|---|---|---|---|
| ① 契约 | — | `21` 八类任务判据：逐问判型 + 选型总表 + 必报量登记 | 先按 `21` 判型填契约，`solver` 交叉验证 |
| ② 建模 | `math-modeling-solver`：12类问题本质 + 95+模型决策矩阵 + 11本Playbook + 22个代码模板 | `07_技能_comp-modeling.md`：可解释性铁律 + 问题分析章 + 基线对比 + §2.0 选型硬规则；`21` 必报量/误配红线；`22` 题型方法细节 | 先跑 `07` 锁国赛门禁 → 调 `solver` 选模型/取Playbook → 方法细节查 `22` |
| ③ 代码 | `solver` 的 22 个 Python 代码模板 | `09_技能_comp-code.md`：数据预处理四步 + 七类质量门 + `all_results.json` 数字源 + §2.5 题型专项纪律（细则在 `22`） | 以 `09` 为准做质量门，代码骨架可借 `solver` 模板 |
| ④ 写稿 | `math-modeling-paper`：章节写法 + 四轮自审 + 去 AI 味指南 | `10_技能_comp-paper.md` + `04_写作铁律`：更严去 AI 规范 + 图表/数字门禁 + §2 摘要四步流程 + §8 灵敏度三档/优缺点比 | 调 `paper` 写稿，套 `10`+`04` 国赛门禁 |
| ⑤ 评审 | `math-modeling-review`：国奖分项打分 + A/B/C/D 类型识别 | `08_技能_auto-review.md` + `05`123条 + 附"八类必报量核查"：数字核对 + 按问核对必报量 | 调 `review` 打分，叠加 `08`+`05` 门禁迭代 |
| 改进模式 | `paper` + `review` | `06_改进论文流程.md` | 写稿/评审阶段同样调真 skill，套国赛门禁 |

> 一句话：真 skill 解决"方法对不对、写法专不专业"，作战包解决"国赛扣不扣分、数字真不真、AI 味去没去"。建模时我会自动把两边串起来，你只管说"按作战包跑第 X 阶段"。

---

## 三、我提前做好的"准备"（减轻你以后负担）

0. **赛题识别与题型手册（2026-08-28 新增，破题与解题的两本"圣经"）**
   - `21_赛题识别八类任务判据.md`：开赛 40 分钟流程（产出「第几问→任务→方法→求解」表）、八类任务判据（题面特征/关键词/方法族/**必报量**/真题落点/常见错法）、四组易混对照、设问关键词对照表、外推 10% 判据与树模型天花板实证、优化规模分档与 gap、线性化三招、整卷统筹两接口、十道真题选型对照、六类高频误配、选题概率账。**①② 阶段第一裁决。**
   - `22_题型流程化解题手册.md`：相关性/评价/聚类/分类/回归预测/ARIMA 六步/规划优化/预处理与降维的完整作业流程，含模型选择条件表、必做检验速查、不推荐清单（灰色关联、外推用树模型、评价用神经网络、summary 截图…）。**③ 阶段按题型照章执行，④ 检验段取材。**

1. **`01_研究契约模板.md` / `02_Claims-Evidence矩阵.md` / `03_实验计划模板.md`**
   → 你每次只需填空，不用从零构思结构。
2. **`04_写作铁律_去AI痕迹.md`**
   → 我写稿时强制守这 7 条，避免"一眼 AI"被评委扣分。
3. **`05_自动评审清单.md`**
   → 我当审稿人时的固定打分表（假设/模型/可解性/结果支撑/格式），可反复跑。
4. **国赛格式规范（格式圣经 `17_国赛Word原模板格式规范.md`）**
   → 来自用户给的「国赛模板（最强版）.docx」实测 + 2023 国一 C050：A4、左右2.70/上下2.54cm、正文宋体**小四12pt**单倍行距（v3 更正，旧“五号10.5pt”作废）、标题黑体、三线表（**程序化加边框**）、图题在下/表题在上、无目录、首页三要素（标题+摘要+关键词）、**无封面信息表**（C050 范式，v3 反转检查）。以 `17` 为唯一裁决，配套 `validate_docx_format.py` 程序化核验。
5. **改进论文流程**（见 `06`）
   → 你发"原优秀论文"，我直接产出「修改总概况 + 原不足 + 正常国赛结构」的改进稿。
6. **可执行的技能提示词（开放、归你所有，四份闭环）**
   - `07_技能_comp-modeling.md` → ② 建模：输出可求解可验证的模型方案。
   - `09_技能_comp-code.md` → ③ 代码求解：把模型转成可运行代码，七类自检，数字进 `all_results.json`。
   - `10_技能_comp-paper.md` → ④ 写稿：按国赛样式 + 去 AI 铁律，数字取自 JSON，产出 `论文.md`。
   - `08_技能_auto-review.md` → ⑤ 评审：当资深评委挑刺并迭代改稿直到定稿。
   - **与已装真 skill 的关系（互补，不重复）**：`math-modeling-solver/paper/review` 提供方法深度（模型矩阵、Playbook、章节写法、国奖打分），07~10 提供国赛专项约束（可解释性铁律 / `all_results.json` 数字源 / 123 条门禁 / 去 AI 味）。调用映射见上文「阶段 ↔ 已装 skill 调用映射」。
7. **A/C 题获奖论文学习结论（2021–2025，已化为知识）**
   - 学习报告：`11_A_C题获奖论文学习报告.md`（题型五年总结 + 质量标杆 + 差距清单 + 选题意略 + A/C 方法工具箱）。
   - 选题意略：综合看 **C题 更匹配队长**（贴合 AI/自动化方向、模型开放容错高、数据预处理正是 `09` 强项）。**2026-09-05 定版：只做 C 题**（用户已定，不再 A/C 横跳）；`13b` 打分表仍用于 C 题与备选间的最终取舍。
   - 已据差距升级技能：`10` 结构→弹性九章（加「问题分析」「数据预处理」章、强化灵敏度与基线对比）；`09` 加数据预处理四步 + 精度一致 + 附录代码；`07` 加问题分析拆解 + 基线对比意识。

8. **123 条全流程自查清单（已融入流程，防低级失分）**
   - 原文存档：`12_123条全流程自查清单_原文.md`。
   - 打勾终检总表：`05_自动评审清单.md` 已重写为逐条打勾版（源自 123 条原文，现扩至 **132 条**；标注 `[人]`/`[自]`/`[评]` 与阶段），第 ⑤ 评审我按【评】挑刺、第 ⑥ 交付前你逐条打勾。
   - 关键规则已固化进执行指令：`07` §2.2（建模失分点）、`09` §0.5（代码纪律）、`10` §7（图表与结果呈现）、`04` 补充写作规范（摘要五要素/术语/参考文献/版式）——让【自】类条目在写稿/求解阶段自动守，不靠临场想。
   - 这份清单补上了作战包此前偏"方法论"而缺的"逐条打勾门"，与 A/C 题国奖标准互相印证。

9. **选题决策打分表（开赛当天定题用）**
   - 文件：`13b_选题决策打分表.md`。
   - 用法：题目一下来，给每道候选题按 7 维度（数据/模型明确度/能力匹配/可解释/创新/时间/风险）加权打分 + 红绿灯决策，**≤2h 内定题、定后不横跳**；附近年 A/B/C/D/E 题型画像速查与 30 分钟团队对齐法。
   - 与 `07` 国赛铁律呼应：默认优先可解释白盒；给你默认建议——C 题数据质量高且看得懂时**无脑优先 C**（最匹配 AI/pandas 方向、容错高）。

10. **国一 C050 对标 + Word 原模板路线（本次改进核心）**
   - `17_国赛Word原模板格式规范.md`：格式圣经（实测自模板+C050），写稿/交付/评审的唯一裁决。
   - `validate_docx_format.py`：程序化逐条格式校验（A4/边距/字体/三线表/结构/封面表…），**⑥ 交付前 fail==0 才过**，不靠记忆。
   - `build_docx.py`：`论文.md` → 套 `17` 规范的 `.docx`（三线表加边框、图题下/表题上、封面信息表）。
   - `19_C050国一论文对标学习报告.md`：它做的 vs 我做的 差异清单与改进落地。
   - **结构已从"方法驱动九章"改为 C050 的"问题驱动"**（按问题分章、五数据侧写、十二附录三件套；问数以 stage1 题目卡 problem_count 为准，六~九/四问仅为 2025C 四问题示例，3 问或 5 问题按序顺延）；摘要改"逐问范式"。

> 以后你只要说"按作战包跑第①阶段，题是这个…"，我就调对应模板开干，不用每次重新定框架。

### 连续模式（极简口令，推荐日常用）
阶段内部仍细分 ①~⑥ 保质量，但你的口令只需三句：**「跑完整流程」+「继续」+「有问题直接说」**。我说"跑完整流程"后自动连跑 ①②③④⑤，只在 4 个必须你拍板的检查点暂停（模型与数据确认 / 评审前快照 / 定稿确认 / 提交终检），你回「继续」即默认认可、开口即改。详见 `15_极简对话速查_连续模式.md`。单阶段口令（`按作战包跑第X阶段`）依然有效，两者不冲突。

---


1. **写前先填三张表**（契约/矩阵/计划）——比直接开写省一半返工。
2. **数字必须可追溯**——所有表里的数都来自一份 JSON，论文不手填。

---

*下一步建议：先把你手上的「题目 / 原优秀论文 / 数据集」发我，我们从 ① 契约 开始实战一遍。*

## 2026-08-31 新增参考文件
- **27_模拟复盘_事故库与提速SOP.md（v2）**：主对话巡检节拍、agent 分工铁律、docx 终检三指标、知网文献人机分工（主路径）、合规边界；§7 为 2023C 增补（口径预演/求解-文档 agent 拆分/页数预算/溯源注释规范/整改后自查，事故 #14~19）——**开赛与每阶段巡检必读 §2/§3/§7**；
- **28_获奖卷图式库_C题2023-2025.md**：五卷获奖原文逐图拆解的 20 张图式卡（A~F 六类）+ scripts/figure_templates.py 可执行模板——**绘图任务书必须指定卡号**。

## 2026-09-02 新增参考文件
- **29_C050对标_呈现层验收清单.md**：2023C 模拟卷 vs 国一 C050 逐句对标的呈现层教训——§A 写作验收十条（④任务书硬性抄入）、§B 评审门禁（⑤逐条打勾）、§C 国一硬伤反向警戒。核心结论：评审对"读不顺"的容忍远低于"数字难复核"，技术领先必须配呈现层功课。

## 2026-09-05 新增（机制升级四件套，stage 模板已全部接线）
- **`30/31_数模模型字典`（，已全量审核）+ `scripts/query_model_dict.py`**：② 选型后查单模型的假设/禁忌/检验，只信"可信"条目（`--only-ok`），赛时零核验负担。
- **`32_命题人评阅要点库.md`**：22 篇命题人官方解析+评分细则蒸馏——14 条铁律、分题型偏好、22 题失分速查表、2025 分值骨架（摘要 10 分/检验显式得分/创新加分）。①审题红线、⑤评分视角终审用。
- **bake-off 选拔赛**（②任务5 + ③任务1 + ⑤第8条）：核心问 2~4 候选统一协议跑分选赢家；**验证段选模、测试段只报告**；落选者一个字不进论文。选型口径=题目适配+可解释+可核验（新颖度落结构层、全卷预算 1~2 处）。
- **逻辑合同体系**（②任务6 + ③任务9 + ⑤第10条）：`LOGIC_CONTRACT.json` 六类元约束（bounds 方向四步/防重复计量/必入模变量/训练区间/限值余量/等价声称）+ `CROSS_PROBLEM_LEDGER.json` 跨问登记 + 方向探针数值证伪 + 确定性闸 `scripts/logic_audit.py`/`cross_problem_check.py`（退出码 1=必修）；⑤ 叠加独立视角六类对抗复核，"任务理解 vs 题目原文"属 fatal。
- **加粗口径勘误**：17 号 §2A v3（克制加粗范式）为准——20 号 L3 旧">3 处"已废；摘要三类锚点（加粗字符约 200）、正文每页 1~4 处、校验器 v3 阈值（正文 10~400 段）。
- **求解时限按 27 号经验**：任务书写明每问时限+主对话巡检收工令，精度优先不降配，不设全局秒数上限；单次求解可中断进度落盘。
