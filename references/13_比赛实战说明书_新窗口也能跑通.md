# 国赛实战说明书（新窗口也能跑通）

> 用途：比赛那天你新开一个窗口/新开一个 workspace，照这份说明书操作，整套"真 skill + 国赛作战包"流程就能跑起来。
> 配套文件：本目录 `00_作战SOP.md` + `07~10` 技能 + `04` 铁律 + `05` 123条终检 + `11/12` 学习资料。

---

## 0. 最关键的一点：新窗口到底丢不丢东西？

结论：**方法引擎不丢，且作战包已封装成技能自动触发（2026-09-05 更新现状）**。

| 资产 | 存哪 | 新窗口能用吗 |
|---|---|---|
| `math-modeling-solver` / `paper` / `review` + `guosai-zuozhanbao`（作战包本体） | `C:\Users\<user>\.zcode\skills\`（ZCode user-level） | ✅ 任何窗口自动可用，说"按作战包"即触发 |

所以：**新窗口第一句话直接说"按作战包跑第X阶段"即可**（guosai-zuozhanbao 技能自动加载 SKILL.md 与全部门禁）。兜底：若触发失灵，把绝对路径 `C:\Users\<user>\.zcode\skills\guosai-zuozhanbao\references\00_作战SOP.md` 丢给我读一次即激活。

---

## 1. 新窗口「开场白」（复制这段，作为第一句发我）

```
按作战包跑完整流程。题是：<题面/需求粘贴在这里> 数据在：<路径或说明>
（兜底写法：你先读取 C:\Users\<user>\.zcode\skills\guosai-zuozhanbao\references\00_作战SOP.md，
并按里面的「阶段↔已装skill调用映射」执行。现在开始【第X阶段】：……）
```

- 跑完整流程把 `【第X阶段】` 换成：`【完整流程，题是……，数据在……】`。
- 改进论文（练习用）换成：`【改进论文模式】，原优秀论文是……，题是……】`（走 `06`）。

---

## 2. 每个阶段「你说什么 / 我做什么」对照

| 你要做的 | 你对我说的口令 | 我自动执行 |
|---|---|---|
| 开赛定题后 | "按作战包跑第①阶段，题是……" | 填 `01` 研究契约 + `02` Claims矩阵，锁范围 |
| 建模 | "按作战包跑第②阶段" | 调 `07`(可解释性/问题分析/基线) + `solver` 选模型 |
| 写代码求解 | "按作战包跑第③阶段，数据在……" | 调 `09`(预处理四步+七类质量门) + `solver` 代码模板，出 `all_results.json` |
| 写论文 | "按作战包跑第④阶段" | 调 `10`+`04`(去AI/图表数字门禁) + `paper`，交 `论文.md` |
| 评审改稿 | "按作战包跑第⑤阶段" | 调 `08`+`05`(数字核对+123条【评】类) + `review`，迭代到致命=0 |
| 编译交付 | "按作战包跑第⑥阶段，编译PDF" | `build_docx.py` 转 .docx（套 17 规范）→ `validate_docx_format.py` 校验 → 脱敏+命名；PDF 由人工在 Word 导出（cumcmthesis LaTeX 仅备选，见 14 号第六节） |

> 一句话开关就是"**按作战包跑第X阶段**"，不用你每次重定框架。

---

## 3. 三天时间轴（甘特参考，按 74 小时估）

| 时间 | 阶段 | 动作 | 门禁 |
|---|---|---|---|
| D日 晚(题目发布) | ① | 通读3题→**≤2h定题**(123条#1)，不横跳 | 选题决策打分表(见§7)；**定题后立刻跑 21 号 §1「开赛40分钟流程」**：0-10min 逐问画设问动词定任务类型 → 10-20min 数据体检 → 20-30min 变量下标与约束草表 → 30-40min 定方法链+最小算例跑通，产出「第几问→任务→方法→求解」总表 |
| D日 夜 | ①② | 写契约+问题分析，定模型方向 | `21` 八类判据逐问判型 + `07` 可解释性铁律 |
| D+1 全天 | ②③ | 数据预处理→求解→出图+JSON | `09` 七类质量门全过；题型流程照 `22` 对应章 |
| D+2 全天 | ④ | 写稿问题驱动结构，守铁律，数字取自JSON | `10`+`04` 去AI终检 |
| D+3 上午 | ⑤ | 我当评委挑刺，你改，迭代 | 致命=0 且 ≥85 分；逐问核对 `05` 附录必报量 |
| D+3 下午 | ⑥ | 编译PDF→脱敏→命名→提前1h提交 | 123条#116~123 |

> 任何单点卡壳 >1h 立刻换思路(123条#39)；核心问题优先，不本末倒置(#40)。

---

## 4. 三人团队怎么用我（CUMCM 是 3 人队）

作战包默认"你(1人)+我(ai)"。真实比赛是 3 人，建议这么分：

- **建模手**（1人）：主导 ①②，和我一起定模型，负责 `01/02/03` 填写。
- **代码手**（1人）：主导 ③，跑 `09` 的质量门，产出 `all_results.json` + 图。
- **论文手**（1人）：主导 ④⑤，写 `论文.md`，用 `05` 123条逐条打勾。
- **我(ai)**：每个阶段被对应的人"按作战包跑第X阶段"唤起，提供方法深度+门禁+评审。
- **全员对齐点**(123条#38)：每天睡前用 10 分钟对齐"假设/模型/结论"，避免三段像三篇论文(#33)。

---

## 5. 赛前 1 周「环境自检」（开赛前必做，否则现场抓瞎）

- [ ] **LaTeX 模板**：`cumcmthesis` 的 `.cls` 已装好、能编译出空白模板（本机 MiKTeX 已装，见 MEMORY）。
- [ ] **Python 环境**：`D:\tools\python` 下 `numpy/pandas/scipy/scikit-learn/matplotlib` 可用（已装）；深度学习类仅 C 题大数据才考虑。
- [ ] **API/网络**：确认比赛场地有网；ZCode 客户端模型可用（技能本身本地运行，无需额外 Key）。
- [ ] **备份机制**：论文目录每天手动 `复制一份带日期` 的副本（见§7 备份漏点）。
- [ ] **承诺书**：从赛事系统下载《承诺书》空白件，赛前打印/电子版备好（见§7）。
- [ ] **作战包就位**：本说明书 + `00` SOP 各存一份到桌面或 U 盘，防 workspace 打不开。

---

## 6. 提交前「终检三连」（123条#116~123）

1. **编译**：`论文.md` → `build_docx.py` 套 17 规范出 .docx → `validate_docx_format.py` fail==0 → 人工在 Word 导出 PDF，检查无乱码/无空白页（cumcmthesis 仅备选）。
2. **脱敏**：全篇搜学校名/队员姓名/队号，删除(123条#78)；确认无我的内部备注残留。
3. **命名+提前交**：按赛事命名规则；**至少提前 1 小时提交**(#121)，提交后确认文件可打开(#122)。

---

## 7. 我替你想的「漏点」——你大概率还没考虑到

2. **作战包没封装成 user-level skill** → ✅ 已完成：注册在 `C:\Users\<user>\.zcode\skills\guosai-zuozhanbao\SKILL.md`，自带触发词与启动逻辑，彻底免"读路径"。
3. **没有"选题决策打分表"** → ✅ 已补：`13b_选题决策打分表.md`（7 维度加权打分 + 红绿灯决策 + 近年题型画像速查 + 30分钟团队对齐法），开赛当天给每题算分定题用。
4. **承诺书/签字漏了** → CUMCM 必须单独上传《承诺书》，且论文内绝不能出现校名人名。赛前准备，提交时别忘。
5. **编译 PDF 这一步没真正接上** → ✅ 已接上：⑥ 主路线 `build_docx.py` → .docx → `validate_docx_format.py`，PDF 由人工在 Word 导出；LaTeX（cumcmthesis.cls 在 scripts/）降为备选，见 14 号第六节。（2026-09-05 更新：旧 build/ 目录已废弃）
6. **没有备份/版本机制** → 3 天高频改动，崩一次全没。建议每阶段完成复制一份 `论文_YYYYMMDD_HH.md`，或 git 提交。
7. **文献获取没规划** → ④需要近3-5年参考文献(123条#72)，赛场能不能上 CNKI/官网没确认。提前存好可用文献源或离线包。
8. **英文摘要不确定** → 部分年份要求英文摘要(#87)。开赛先确认今年规则，要就同步写中英文对照。
9. **改进论文模式别混进正式赛** → `06` 是"练手分析往年论文"用的，正式比赛走 ①②③④⑤⑥，不要把"修改总概况"写进正式稿。
10. **网络/API 断的兜底** → 若赛场无网，真 skill 和我的能力都用不了。需备"离线也能跑"的最小方案（本地 Python + 手写的 07~10 打印稿当 checklist）。
11. **三人并行 ≠ 三窗口** → 别三个人各开一个窗口各让我干，会逻辑割裂(#33)。建议一个人当"调度员"统一发口令。
12. **时区/题目发布时间** → CUMCM 通常周四晚发题，确认今年 exact 时间，提前 30 分就位。

---

## 8. 已完成：user-level skill 封装（彻底解决"新窗口"）

作战包已封装为 ZCode 技能 `guosai-zuozhanbao`（`C:\Users\<user>\.zcode\skills\guosai-zuozhanbao\SKILL.md`，触发词：国赛/数模/数学建模/按作战包/CUMCM）。封装后：
- 任何新窗口直接说"按作战包跑第X阶段"即可，不用再贴绝对路径；
- 三个真 skill 的方法深度 + 作战包国赛门禁自动串联。

（全部 12 个漏点均已闭环：#1 开场白/#2 封装 skill/#5 编译 此前已解决；#3 选题打分表 现已补 `13b`。剩余 #4 承诺书、#6 备份、#7 文献、#8 英文摘要、#10 离线兜底 为赛前准备项，非流程缺漏。）

## 附：编排巡检与分工铁律（2026-08-31 新增，详见 27 号）
- 子 agent 派出后主对话每 30~60 分钟巡检一次（产物时间戳+进程），超时限立即强制收工令；禁止派完就等通知。
- 主对话写文件仅限编排产物（PROGRESS/合并/单点急救<15 分钟）；一切成规模代码与文档由子 agent 承担。
- 求解前预注册定稿口径，第一遍按终版口径跑，禁止先跑再定稿重滚。
